{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Gemini Provider Options",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "api_key_env": {
      "type": "string",
      "description": "Environment variable name that contains the Gemini API key."
    },
    "api_version": {
      "type": "string",
      "description": "Gemini API version path segment (default: 'v1beta')."
    },
    "embedding_dimensions": {
      "type": "integer",
      "minimum": 1,
      "description": "Override embedding vector dimensions reported by the model handle."
    }
  }
}
