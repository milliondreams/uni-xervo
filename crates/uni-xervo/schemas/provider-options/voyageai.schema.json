{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Voyage AI Provider Options",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "api_key_env": {
      "type": "string",
      "description": "Environment variable name that contains the Voyage AI API key."
    },
    "embedding_dimensions": {
      "type": "integer",
      "minimum": 1,
      "description": "Override embedding vector dimensions reported by the model handle."
    }
  }
}
